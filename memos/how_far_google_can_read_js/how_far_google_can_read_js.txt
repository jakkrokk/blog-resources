●window.locationを用いたリダイレクト処理のテスト
●JavaScript でのリンク生成テスト
●動的に動的コンテンツを埋め込むテスト
●動的にメタデータやエレメント等を埋め込むテスト


●window.locationを用いたリダイレクト処理のテスト
--------------------------------------------------------
(一般的なjavascriptによる転送)
⇒301として扱い、インデックスも変更された

・補足テスト
上記をオーソリティのあるページから新規ページ（同一コンテンツ）に行った
⇒期待通りインデックスも変更され順位の下落もなかった。
--------------------------------------------------------


●JavaScript でのリンク生成テスト
--------------------------------------------------------
ドロップダウンボックスにあるリンクを動的に書きかえ、
それを読めるかのテスト
⇒リンクは完全に読み込まれクローラはたどっていった。

・補足テスト
プレーンテキストにリンク処理をjavascriptで行ってみた
⇒上記同様全て読み込まれた。
※Aタグじゃなくて、spanタグとかでリンクにしても大丈夫ということに。
※リンクジュースが渡ったかのテストはしていない
--------------------------------------------------------


●動的にコンテンツを埋め込むテスト
--------------------------------------------------------
1. HTMLのどこかにかかれているコンテンツの表示ON/OFF
2. 外部ファイルにかかれているコンテンツの動的読み込み
⇒両方ともにきちんとインデックスした。

・補足テスト
グローバルナビを全てjavascriptで動的に書き出し、
リンク自体もjavascriptで処理するようにした。
⇒クローラは全て正しく認識した。
--------------------------------------------------------


●動的にメタデータやエレメント等を埋め込むテスト
--------------------------------------------------------
タイトルタグ、ディスクリプション、ロボット、
キャノニカルのタグを動的に追加した。
⇒全て正しく認識した。

・補足
noindex,nofollowとHTMLに書いておいて、
動的にindex,followにするなど、
相反する書き換えを行った場合のテストはまだ行っていない。

nofollowについて
上記のテストをnofollowでも行ったがこちらは結果が異なった。
HTMLにない状態でjavascriptでnofollowを書き足しても、
正常に動作せず、ページは処理された。
※この後の考察は間違っているので削除
--------------------------------------------------------

結論ほぼほぼajax周りはシミュレートした後でインデクサが走るようです。
もっともnofollowの件のように、
インデクサとは別処理になっているようなやつは
注意が必要っぽいですね。

